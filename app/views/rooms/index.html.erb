
      <div class="popular-room-box">
        <p>作成した相談部屋</p>
        <% @rooms.each do |room| %>
        <div class="room-box"><a href="/rooms/<%= rooms.id %>"><%= room.room_name %></a></div>
        <% end %>
      </div>